<Window x:Class="WpfGroup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfGroup"
        mc:Ignorable="d"
        Title="WPFGroup" Height="400" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ToolBar Grid.Column="0" Grid.Row="0">
            <Button Click="Button_Click" FontWeight="Bold">Ставка</Button>
            <Separator />
            <Button FontWeight="Bold" Click="Button_Click_1">Должность</Button>
            <Separator />
            <Button FontWeight="Bold" Click="Button_Click_2">Отчёт</Button>
        </ToolBar>
        <Grid Grid.Column="0" Grid.Row="1" x:Name="MineFrame"></Grid>
    </Grid>
</Window>
